version: 2
updates:
  # Update Go dependencies if they're found weekly.
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "🚀 merge it!"
  # Update Github Actions if they're found weekly.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "🚀 merge it!"
